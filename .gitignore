# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cloudflare / Wrangler
.wrangler
.dev.vars
.dev.vars.*
# ...but the template must be committable, or a fresh clone has no way to know
# which secrets are required. `.dev.vars.*` above would otherwise swallow it.
!.dev.vars.example
worker-configuration.d.ts

# TanStack Router (generated)
src/routeTree.gen.ts
.tanstack

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
